import { Router } from "express";

const router = Router();

router.post("/secret-chat", async (req, res) => {

  const { messages,userid ,password} = req.body;

    // Validate the password    
    






})